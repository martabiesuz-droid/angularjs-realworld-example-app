services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "4200:80"
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:80/ 2>/dev/null || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
